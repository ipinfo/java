sudo: false
language: java
dist: trusty
jdk:
  - oraclejdk8

install:
  - mvn -B install -U

after_success:
  - util/deploy.sh
  - util/deploy_docs.sh

branches:
  only:
    - master

cache:
  directories:
  - $HOME/.m2
