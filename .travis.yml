sudo: false
language: api

jdk:
  - oraclejdk8
  - oraclejdk10

install:
  - mvn -B install -U

after_success:
  - util/deploy_snapshot.sh
  - util/deploy_docs.sh

branches:
  only:
    - master

cache:
  directories:
  - $HOME/.m2


